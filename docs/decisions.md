# Platform decisions

Platform-level choices with their evidence. Physics choices that belong to a measurement live in
the channel YAML with a `status`; entries here explain the `default` values and record engineering
decisions. Format: date | scope | decision | evidence / rationale | where.

2026-09-04 | architecture | The platform is a separate repository that *imports* the MINERvA exploration repo (benchmark engine, paper releases, selection constants) and never edits it | The exploration repo is an audited, manifest-backed record with its own operating rules; the platform is experiment-agnostic machinery that must not carry that repo's decisions into code | `ndp/compare/minerva_bridge.py`, `ndp.yaml`

2026-09-04 | data model | One columnar `TruthTable` (GeV, GeV², mm; jagged final state as CSR) is the contract between generators, channels, surrogates and comparisons | Keeps the core numpy-only; every generator/experiment needs one adapter, not one per stage | `ndp/events.py`

2026-09-04 | selection | The MINERvA CC-inclusive selection is a vectorised transcription of `tools/cc_inclusive_selector.py`, certified row-by-row (0 mismatches on the 6304-row data file and 20k MC rows, including the failing-cut label) and against the audited run's counts (844/835 data, 43643/43361/43266 MC, 43175/91/212 migration, 41948/41922 numerator, 65041/65003 denominator, all reproduced with `frame: detector`) | uproot bulk reads make the surrogate build a 30-second job; the per-entry tool stays the reference | `ndp/adapters/minerva_anatuple.py`, `tests/test_minerva_certification.py`

2026-09-04 | phase space (channel default) | Truth lepton kinematics are evaluated in the **NuMI beam frame** (rotation about x by MAT's `numi_beam_angle_rad = -0.05887`) for `minerva_me_cc_inclusive_ptpz` | On MC run 110040 the reco−true muon pT core width (1.4826·MAD) is 0.278 GeV with detector-frame truth and 0.061 GeV with beam-frame truth (wrong-sign rotation: 0.465 GeV); same-cell fraction of the 224-cell migration 0.098 → 0.396; median pT bias −54 MeV → +1.6 MeV. The reco `muon_thetaX/Y` branches are therefore beam-relative, as MAT's `GetThetalepTrue` convention implies. The audited runs 2026-06-09/-19 in the exploration repo used the detector frame ("small phase-space-edge caveat"); their migration and unfolded shapes carry this mismatch — **reported to the user, who owns that repo; not changed here.** `frame: detector` reproduces their numbers. The archived pure-Python reproduction (`xsec/kinematics.py::true_theta_p` at tag `ndp-minerva-xsec-final`) already applied this RotateX beam rotation, corroborating the convention | `channels/minerva_me_cc_inclusive_ptpz.yaml`, `tests/test_minerva_certification.py::test_beam_frame_sharpens_resolution`

2026-09-04 | surrogate (binned) | `P[:, j] = M[:, j]/num[j]` (columns sum to ≤ 1) and selected events whose truth fails the phase space or the true grid are an additive, POT-scaled background together with non-signal events | With these definitions folding the training MC's own truth reproduces its selected in-grid counts exactly (43361.0, max cell deviation 0) — an exact closure the platform tests enforce. The alternative (renormalising columns to 1, migration from all selected signal) redistributes real losses and double-counts feed-in | `ndp/surrogate/binned.py`, `tests/test_surrogates.py`, `tests/test_minerva_certification.py`

2026-09-04 | surrogate (parametric) | Per-true-cell robust location/width (median, 1.4826·MAD) of reco−true pT and reco/true p∥, Gaussian core only | Mean/std are dominated by the 1/|q/p| tails (std of reco−true pT is 147 GeV on this MC); the core widths (0.27 GeV detector frame → 0.06 GeV beam frame for pT; 8% for p∥) are what a smearing model should carry. Tails are a documented limitation, to be addressed by a flow-type surrogate on the same interface | `ndp/surrogate/parametric.py`

2026-09-04 | phase space vs geometry | The fiducial-vertex requirement is applied only to samples that carry detector geometry (`meta.has_geometry`); point-target generator runs are taken as generated on the fiducial target whose mass is `n_nucleons` | A vertex cut on a sample with no vertices would remove everything (observed: 0 events in phase space on the first GENIE run) while it is not a kinematic restriction | `ndp/channels/registry.py`

2026-09-04 | flux | The published 2110.13372 supplemental Table I (νμ/m²/10⁵ POT/GeV, 0–100 GeV) drives GENIE and cross-checks Φ; Φ itself stays the published 6.32e-8 ν/cm²/POT the exploration repo adopted | Table integral 6.265e-8 (ratio 0.991), the shortfall being two-decimal truncation above ~50 GeV. The MAT flux files at hand are `MnvH1D` objects unreadable without MAT libraries on this node | `resources/flux/`, `ndp/theory/flux.py`

2026-09-04 | GENIE normalisation | σ_avg/nucleon = ∫Φ Σ_i (w_i/A_i) σ_i^{CC}(E) dE / ∫Φ dE from the spline XML, target-mix weights are mass fractions | GENIE's `GMCJDriver` treats `-t` weights as density-weighted path lengths and divides by A (so they are mass fractions), matching the `H2O = O16[0.8888],H1[0.1111]` convention; the resulting 4.49e-38 cm²/nucleon for CC νμ at ⟨E⟩≈6 GeV is the textbook magnitude | `ndp/theory/splines.py`, `ndp/theory/generator.py`

2026-09-04 | GENIE runs | Productions are cached by a fingerprint of the physics spec + flux source; logging verbosity (`--message-thresholds Messenger_laconic.xml`) is outside the fingerprint | The first production wrote 790 MB of stdout per 30k-event job; verbosity is not physics | `ndp/theory/generator.py`

2026-09-04 | comparisons | Report unfolded (paper covariance, and paper + model-stat) and folded (−2lnL and Pearson with MC-stat) side by side; never a combined verdict | The two modes answer different questions (published unfolded result vs detector-level counts); the exploration repo's benchmark contract makes the same choice | `ndp/compare/`, `ndp/compare/report.py`

2026-09-04 | environment | The platform's environment is a pixi project (conda-forge): Python stack + ROOT 6.40 + GCC 15.2 + GSL/log4cpp/libxml2/LHAPDF 6, and GENIE Generator R-3_06_02 is built in place under `external/` with Pythia6 supplied by luketpickering/ROOTEGPythia6 (ROOT ≥ 6.30 dropped EGPythia6; conda ROOT ships Pythia8 only). Pythia6 is compiled for `x86-64-v2` rather than `-march=native` so binaries stay portable; GENIE's `Make.include` gets a two-line patch (ROOTEGPythia6 include/lib dirs) applied by `scripts/build_genie.sh`, never by hand | Requested by the user 2026-09-04; mirrors the proven nc1p recipe (ROOT 6.40 + gcc 15.2 + ROOTEGPythia6), but with upstream GENIE 3.6.2 and LHAPDF 6 from conda instead of the MicroBooNE fork and a source-built LHAPDF 5. R-3_06_02 matches the user's spack install and the CVMFS G18_02a spline family | `pixi.toml`, `activate.sh`, `scripts/build_*.sh`

2026-09-04 | legacy artifact surrogate | `surrogates/.../binned_from_run_2026-06-19` (rebuilt from the audited run's exported migration/efficiency, detector frame, migration over all selected signal) is kept as a re-indexing check and fallback only | It reproduces the run's 43175-pair migration bit for bit but is not closure-consistent under the platform's conventions | `ndp/surrogate/binned.py::from_run_artifacts`

2026-09-04 | GENIE splines | Non-default tunes get splines generated with the in-repo GENIE (`pixi run make-splines`, one gmkspl per tracker nuclide, merged with gspladd); the generator runner refuses to start when the spline file lacks a nuclide of the target mix or carries a different tune header | The CVMFS `v3_06_02_sbn2` G18_10a_02_11b set has no Cl35 splines and was produced by an SBN pre-release, and GENIE's only symptom is an opaque `Assertion fUseSplines failed`. The default G18_02a set (CVMFS `v3_06_00`, tune header verified) stays the site default | `scripts/make_splines.sh`, `ndp/theory/splines.py::check_spline_coverage`

2026-09-04 | environment | NuWro (GitHub master, tagged 25.11.1), GiBUU release 2025 (+ buuinput 2025) and ACHILLES v0.3.1 are built in the same pixi environment under `external/` (`pixi run build-nuwro|build-gibuu|build-achilles`); their outputs are ingested by adapters rather than by runners for now | Requested by the user 2026-09-04. NuWro's Makefile already supports ROOTEGPythia6; GiBUU builds with the pixi gfortran exactly as in nc1p; ACHILLES needs `-DCMAKE_POLICY_VERSION_MINIMUM=3.5` under CMake 4 and must fetch fmt/spdlog/yaml-cpp/HepMC3 itself (it aliases the fetched targets by plain name, so conda's imported targets break the configure). Adapters first because comparing existing samples is immediately useful; runners follow the GENIE one | `scripts/build_*.sh`, `ndp/adapters/{nuwro_root,gibuu_finalevents,nuhepmc}.py`

2026-09-04 | adapters | Generator process labels map onto the five NDP codes; GiBUU's non-resonant pion backgrounds (32/33/37) count as DIS, undecayed GiBUU N* in the final state count as nucleons; the generator's own code is always kept | Cross-generator comparisons need one vocabulary; GENIE files its non-resonant background under DIS, so the same choice keeps GENIE and GiBUU comparable. Raw codes stay available for anything finer | `ndp/adapters/gibuu_finalevents.py`, `ndp/adapters/nuhepmc.py`

2026-09-09 | comparisons | The **folded** (forward-folded) comparison is the platform's primary mode and comes first in every report; the unfolded comparison against the published cross section is attached only on a channel's `published` measurement | The workflow is data ← surrogate(official MC) ← theory: the model is folded to reco space, the data are never unfolded here. User-defined observables have no published unfolded result, so folded is the only mode that exists for them; putting it first keeps every report the same shape. Numbers are unchanged by the reordering (regression run `2026-09-09_reference_mc_genie2126__*` reproduces the 2026-09-04 scorecard exactly) | `ndp/pipeline.py`, `ndp/compare/report.py`

2026-09-09 | measurements | Observable pairs live in `measurements/<channel>/<name>.yaml` (or a channel's inline `measurements:`), never in code; each axis has a truth side and a reco side, either a registry name or an expression; the channel's `binning` is its `published` measurement and carries the reco mapping (`reco:`) and a plotting flag (`log:`) — engineering fields, not physics | Lets an analyst compare a model in any reconstructed quantity without editing a channel's decided values; expressions on both sides mean new observables need no code (`"reco_E_mu + reco_recoil_E"`). Example measurements are marked `status: example`; whoever adopts one owns its edges | `ndp/channels/measurements.py`, `channels/minerva_me_cc_inclusive_ptpz.yaml`

2026-09-09 | reco cache | The adapter caches a fixed, versioned set of reconstructed columns per AnaTuple (`RECO_CACHE_COLUMNS`, version 2): muon p/θ/pT/p∥/E and projected angles, MINOS p and q/p, `MasterAnaDev_recoil_E`, `_recoil_passivecorrected`, `blob_recoil_E`, `recoil_E_polylinecorrected`, `MasterAnaDev_E/W/x/y`, `MasterAnaDev_visible_E`, `n_prongs`, `MasterAnaDev_hadron_number`, vertex. `MasterAnaDev_Q2` is not cached (0 % populated on the selected sample); `MasterAnaDev_hadron_recoil_CCInc` ≡ `MasterAnaDev_recoil_E` and `_hadron_recoil_default` ≡ `_recoil_passivecorrected` (identical medians and percentiles on data and MC) so only one of each pair is kept | Reading the 21 GB MC tuple costs ~30 s for these scalars; user observables need them without a re-read. Every cached branch was checked to be ≥ 99 % populated on the selected data and MC (2026-09-09 survey) | `ndp/adapters/minerva_anatuple.py`

2026-09-09 | surrogate closure | `ndp surrogate build` prints and `tests/test_measurements.py` enforces closure per measurement: the binned response must reproduce the training MC's selected in-grid counts exactly; the parametric surrogate is compared with the selected *signal* (it predicts no background) and reported, not enforced | Same convention as the published grid; the parametric model's Gaussian core is a documented approximation (totals agree to 0.2 %, single cells to 25–50 % where migration is tail-dominated) | `ndp/surrogate/build.py::closure`

2026-09-14 | 1mu1p channel | **Reference analysis = arXiv:2503.15047** (MINERvA CCQE-like on C/CH/H₂O/Fe/Pb vs muon, proton and TKI kinematics, ME ν-mode, 10.61e20 POT); the truth-level signal is the paper's definition verbatim: one μ⁻ with θ_μ < 17° to the beam and 2 < p_μ < 20 GeV/c, ≥ 1 proton with θ_p < 70° and 0.5 < p_p < 1.1 GeV/c (leading = highest-momentum proton in the window), no mesons, no baryons heavier than the neutron, no photons > 10 MeV | The user chose this paper as the "1mu1p" reference on 2026-09-14 (the alternatives 1805.05486 / 1409.4497 are LE-beam; 2203.08022 / 1811.02774 have no proton requirement). It uses the same ME neutrino sample as the reproduced inclusive paper and releases absolute CH cross sections with covariances for 14 observables (edges verified from `anc/tki_release.root`). Extraction: MINERvA repo `papers/minerva/2503.15047/` | `channels/minerva_me_ccqelike_1mu1p.yaml` (signal: decided), `ndp/channels/signal.py`

2026-09-14 | 1mu1p channel | Final-state classes are PDG **ranges**, not an enumerated list: meson = 100 ≤ \|pdg\| < 1000; heavy baryon = 1000 ≤ \|pdg\| < 10000 excluding p and n (antinucleons are *not* heavier than the neutron and are not vetoed); photon veto on `fs_E` > 10 MeV; neutrons, nuclear remnants (≥ 1e9), GENIE bookkeeping particles (2000000101) and secondary charged leptons are ignored; the muon is the primary lepton (`lep_pdg == 13`), not a count of FS muons | The paper states classes, not codes. On the open-data MC (397,604 νμ CC events) this agrees with MAT's enumerated `IsQELike` (`MAT-MINERvA/universes/CCQE3DFitsSystematics.cxx`) except for 6 events: 2 with an anti-Λ (−3122) and 1 with an anti-K⁰ (−311), which MAT's list omits but the paper's wording vetoes, and 3–4 with a second FS muon, which MAT rejects (n_μ == 1) and this definition keeps. Recorded in `runs/2026-09-14_signal_minerva_me_ccqelike_1mu1p/summary.json` (`mat_isqelike_parity`) | `ndp/channels/signal.py::fs_classes`, `ndp/diagnostics.py::mat_isqelike`, `tests/test_signal.py`

2026-09-14 | 1mu1p channel | Proton angles are evaluated in the channel's `phase_space.frame` with the same rotation as the lepton (`frame: beam`, platform default with the 2026-09-04 evidence) | The paper measures both angles "with respect to the beam"; a single frame per channel keeps the muon and proton windows consistent. Detector-frame counts differ (the 70° proton edge moves by the 3.4° tilt) and are available by setting `frame: detector` | `ndp/channels/signal.py::rotate_to_frame`

2026-09-14 | architecture | A channel's `signal:` block carries a `type` dispatched in `ndp.channels.signal` (`cc_lepton` default = the certified inclusive behaviour; `minerva_ccqelike_1mu1p`); observables may take the channel's signal block (`proton_p`, `proton_theta[_deg]`, `proton_pT`, `n_protons_in_window` use its proton window); `ndp signal --channel <c>` applies the definition to the cached MC and writes a manifest-backed diagnostics run | Keeps the physics parameters in the manifest and the certified inclusive numbers unchanged (all 35 pre-existing tests pass); the diagnostics run is the record the certification test pins (all-cache cutflow 397604 → 224629 → 74184 → 21974 → 21973 → 21369; 5392 inside the tracker fiducial) | `ndp/channels/registry.py`, `ndp/channels/observables.py`, `ndp/diagnostics.py`, `ndp/cli.py`

2026-09-14 | 1mu1p channel | **TKI observables** (`dpT`, `dpTx`, `dpTy`, `dalphaT[_deg]`, `dphiT[_deg]`, `dpL`, `pn`) follow Lu et al. PRC 94 (2016) 015503 / Furmanski & Sobczyk PRC 95 (2017) 065501 as the paper cites them: δp_T = \|p_T^μ + p_T^p\|, δα_T = ∠(−p_T^μ, δp_T), φ_T = ∠(−p_T^μ, p_T^p), **δp_Tx = (ẑ × p̂_T^μ)·δp_T, δp_Ty = −p̂_T^μ·δp_T**, δp_L = R/2 − (m_A'² + δp_T²)/(2R) with R = m_A + p_L^μ + p_L^p − E^μ − E^p, p_n = √(δp_T² + δp_L²); ẑ = neutrino direction in the channel's frame, proton = the signal block's leading proton | arXiv:2503.15047 prints δp_L, p_n and describes δp_Tx/δp_Ty in words; its Fig. 1 draws δp_Ty along −p_T^μ and δp_Tx to the right with the muon pointing down, and the released δp_Ty grid (−6.5 to 3 GeV/c) has its long tail negative, both matching the Lu sign. MAT's legacy `MnvRecoShifter::Calc_tki_vars` uses the opposite sign for both components (x̂ = p̂^μ × ẑ, ŷ ∥ +p_T^μ), so it is not the paper's convention. Verified by unit identities (exact p_n recovery for a 4-momentum-conserving knockout event; frame-rotation consistency) | `ndp/channels/observables.py`, `tests/test_tki.py`

2026-09-14 | 1mu1p channel (default) | Nuclear masses for δp_L / p_n live in the channel manifest (`observable_params.tki`): m_A = 11.174864 GeV (¹²C nuclear mass = 12 u − 6 m_e), m_n = 0.93956542 GeV, b = +27.13 MeV, so m_A' = 10.262429 GeV; applied to every event, hydrogen included | The paper states no values; the LE analysis (arXiv:1805.05486) gives m_A' = m_A − m_n + b with b = 27.13 MeV for carbon from the Furmanski–Sobczyk excitation model. The code refuses to compute δp_L / p_n without the block, so no constant is ever implied by the platform | `channels/minerva_me_ccqelike_1mu1p.yaml` (status: default), `ndp/channels/observables.py::_tki_masses`

2026-09-14 | reco cache | **Cache version 3** adds the MasterAnaDev leading-proton candidate (`reco_proton_{p,px,py,pz,E,T}` from `_fromdEdx`, beam-frame `reco_proton_theta_beam`, scores 1/2, pion score, start/end z, pattern-rec, `reco_proton_exiting` = `MasterAnaDev_hadron_isExiting[0]`, `reco_proton_truth_pdg` = `proton_prong_PDG`), `reco_n_michel` (`improved_nmichel`), `reco_n_iso_blobs[_all]` (`n_nonvtx_iso_blobs[_all]`), `reco_iso_blobs_E`, `reco_n_sec_protons`, the detector-frame muon components `reco_mu_{px,py,pz}`, `reco_minos_matched`, `reco_n_dead_discr`; tuple sentinels (−9999, and −1 for the end point) become NaN; the MC reco-side truth table now carries the final-state particles | Needed for any tracked-proton selection and for reco TKI. Verified on the open-data MC: `MasterAnaDev_proton_theta` equals the angle of the detector-frame (Px,Py,Pz) after the NuMI beam rotation (median difference 0.0000°), i.e. the same convention as `muon_thetaX/Y` vs `MasterAnaDev_leptonE`; secondary protons (`sec_protons_P_fromdEdx`) never exceed the primary candidate's momentum (0 of 6601 events), so the primary candidate is the leading proton. The inclusive channel's certified counts (844 / 43643) are unchanged | `ndp/adapters/minerva_anatuple.py`, `tests/test_selection.py`

2026-09-14 | architecture | Reconstruction-level selections are dispatched by `selection.name` (`ndp/channels/selections.py`): `minerva_cc_inclusive_v1` = the cached, certified flag; `minerva_ccqelike_1mu1p_v0` = a cutflow evaluated on the v3 cache with every cut value read from `selection.params`. `folded.data_reco_cells` and the surrogate builder use `select(channel, r)` instead of the cached flag; reco observables take the channel's `observable_params` | A second channel needs its own selection without touching the certified one; keeping cut values in the manifest keeps them auditable | `ndp/channels/selections.py`, `ndp/compare/folded.py`, `ndp/surrogate/build.py`

2026-09-14 | 1mu1p channel (default) | **Reco selection `minerva_ccqelike_1mu1p_v0`** = the paper's cut list in the paper's order (tracker fiducial, MINOS-matched μ⁻, dead-time, muon window 17° / 2–20 GeV/c, ≥1 proton candidate, contained, proton dE/dx score, proton window 90° / 0.4–1.3 GeV/c, no Michel electron, ≤ 1 isolated non-vertex blob), with `proton_score1 > 0.35`, `require_contained: true`, `n_nonvtx_iso_blobs` (not `_all`) as defaults | The paper prints the cut list but not the score threshold, the containment criterion or which blob counter; the muon and proton reco windows come from commented-out tex lines. Evidence for the defaults (`runs/2026-09-14_selection_minerva_me_ccqelike_1mu1p_2`, MC run 110040, data run 10066 at 2.05e17 POT): score scan purity/efficiency 0.333/0.470 (no cut) → 0.454/0.389 (0.2) → **0.486/0.352 (0.35)** → 0.503/0.307 (0.5) → 0.515/0.264 (0.6) → 0.537/0.131 (0.8) against the paper's 0.60/0.28; no threshold reaches the paper's purity, so the gap is elsewhere (open question). Containment removes 3% of candidates; `n_nonvtx_iso_blobs` and `_all` agree on 95% of candidate events. Selected: 64 data, 3904 MC (80.1 scaled), data/MC 0.80 with unweighted CV MC | `channels/minerva_me_ccqelike_1mu1p.yaml` (selection: default), `ndp/adapters/minerva_anatuple.py::ccqelike_1mu1p_cutflow`, `tests/test_selection.py`

2026-09-14 | 1mu1p channel | The 14 released CH grids of arXiv:2503.15047 are measurement manifests `measurements/minerva_me_ccqelike_1mu1p/*.yaml` (status user; edges from `anc/tki_release.root`), each pairing a truth observable with its reco counterpart (`reco_dpT`, `reco_dalphaT_deg`, …, computed from the cached muon and proton candidate in the beam frame with the truth-level formulas) | The reproduction compares data and MC on the paper's grids; `ndp selection --channel <c>` draws every one with the MC split by truth category (signal by GENIE process; background by final-state pion content) | `measurements/minerva_me_ccqelike_1mu1p/`, `ndp/channels/reco_observables.py`, `ndp/diagnostics.py::run_selection_comparison`

2026-09-14 | grid processing | **Streamed per-file processing on the FNAL grid** reuses the cache builder unchanged: `build_cache` accepts `root://` URLs (never `Path()`ed), opens each AnaTuple once, records POT / entry totals / a remote fingerprint (same head+tail sha256 recipe through uproot's byte source) and, with a channel, the truth signal cutflow, the reco selection cutflow and derived-column skims in a `manifest_<tag>.json` sidecar (`ndp.grid.process_file`, worker `grid/worker.sh`). Payload = a slim pixi environment (`[feature.grid]`: python, numpy, uproot, awkward, xrootd, fsspec-xrootd, pyyaml; 388 MB) + the package, tarred as-is and published via RCDS with the jobsub-lite skill (copied into `.claude/skills/jobsub-lite`, state in `.jobsub/`) | Certified 2026-09-14 by streaming the published twins of the two local files through the grid environment: every array of `reco_data10066`, `reco_mc110040`, `truth_mc110040`, `reco_mc110040_truthcols` byte-identical to the local caches; cutflows `[6304 … 64]`, `[186205 … 3904]`, signal `21369 / 5392`; MC file 70 s wall, 1.26 GB peak RSS, 332 MB requested of 21.5 GB; data file 4 s, 3.7 MB. Only ~330 GB leave dCache for the whole FHC campaign instead of ~20 TB | `ndp/adapters/minerva_anatuple.py`, `ndp/grid/`, `grid/`, `pixi.toml`, `tests/test_grid.py`

2026-09-14 | grid processing (default) | Per file the job keeps the **full v3 caches** (archive, ~300 MB per MC file) **and derived-column skims**: `truth_<tag>_skim.npz` = CC events with the true vertex inside an enlarged tracker box (z 5880–8522 mm, apothem 900 mm, a superset of every channel fiducial), fs_* dropped, per-event columns `n_meson, n_heavy_baryon, n_photon_hard, n_proton, n_neutron, n_pi_charged, n_pi0, E_avail, E_had_fs, lp_*` (leading proton in the channel's window and frame) and `signal_<type>` added with `meta.derived` recording frame / window / threshold / channel; `reco_<tag>_truthcols_skim.npz` likewise for every reco row | Skims are 14 + 10 MB per MC file (124 622 of 544 600 truth rows for run 110040) so a playlist merges in RAM; `signal.py` / `observables.py` read the derived columns when fs_* are absent and refuse a skim derived for another frame or window (tested to reproduce the FS path exactly on toy tables). The skim box and the CC-only row selection are physics defaults | `ndp/adapters/minerva_anatuple.py::derive_fs_columns/skim_truth`, `ndp/channels/signal.py`, `ndp/products.py`

2026-09-14 | grid processing (default) | Job granularity 8 MC files / 100 data files per process (~10 / ~7 min), requests MC `--memory 4000MB --disk 4GB --expected-lifetime 3h`, data `3000MB / 2GB / 2h` (a 1500 MB data job was held while importing the environment from cold CVMFS: ~1.45 GB charged), `--onsite`; concurrency ≤ 20 processes for playlist 1A, then ≤ 50, never > 100; outputs to `/pnfs/dune/scratch/users/liangliu/ndp-stream/<campaign>/<worklist>/<%04d>/`, harvested promptly to `<data_dir>/products/<beam>/<playlist>/files/<tag>/` and merged per playlist and kind (`ndp data merge`, products `reco_<pl>_<kind>.npz`, `pot_<pl>_<kind>.json`, MC skims) with POT = fsum of the sidecars | Measured timings above ×1.3 headroom; the OpenData pool throughput is unknown, so the first playlist measures per-job MB/s from the sidecars before scaling. Scratch is evicted, hence the prompt harvest | `ndp/grid/campaign.py`, `grid/README.md`

2026-09-14 | architecture | **Consumers load tables through `ndp.products`**: playlist products when the channel's `data:` block lists `playlists` (+ `beam`, `products_dir`), else the per-file caches; POT always comes with the table (reco/truth meta, sidecar, merged `pot_<pl>_<kind>.json`), never by reopening an AnaTuple. `TruthTable.concatenate` sums POT for POT-normalised tables and records the sources; `select` is vectorised | Multi-file playlists must be first-class for every stage (diagnostics, surrogate build, folded comparison, reference MC) without touching the certified single-file behaviour (all pre-existing tests unchanged) | `ndp/products.py`, `ndp/events.py`, `ndp/diagnostics.py`, `ndp/surrogate/build.py`, `ndp/compare/folded.py`, `ndp/theory/models.py`

2026-09-14 | grid processing | **Grid smoke test passed** (campaign `smoke_2026-09`: 3 data files in one process, run 110040 in another; payload `ndp-stream-v1`, then v2): the workers' `reco_data10066`, `truth_mc110040` and `reco_mc110040_truthcols` tables are byte-identical to the local caches; `reco_mc110040` differs only in the derived muon angle columns (`reco_theta`, `reco_pT`, `reco_pz`) at ≤ 4.4e-14 relative — CPU-dependent rounding in numpy's transcendental functions on the worker (fnpc19004) versus the EAF node; every raw branch column is identical. Timings on the worker: MC truth 38.8 s, reco 48.8 s, derive 8.1 s; data files ~2–3 s each; cold-CVMFS environment import ~3 min per process | Two defects found and fixed by the smoke test: (1) `-f file://` job inputs are fetched by the worker with `dd` from the submission path and fail off-site — worklists are now uploaded to PNFS scratch (`ndp grid stage-worklists`) and passed as `-f /pnfs/...`; (2) the reco-side truth skim had been row-selected like the Truth-tree skim and no longer aligned with the reco table — it now keeps every row (`skim_truth(keep_all_rows=True)`), and `merge_playlist` refuses misaligned files. A data job requested at 1500 MB was held (~1.45 GB charged during the environment import): requests are 3000 MB (data) / 4000 MB (MC) | `grid/campaigns/smoke_2026-09`, `.jobsub/runs/ndpstream-2026-09-14/smoke2_*`, `ndp/grid/campaign.py`, `ndp/adapters/minerva_anatuple.py`, `ndp/products.py`

2026-09-14 | grid processing | **Playlist 1A processed on the grid** (campaign `fhc_2026-09`, payload `ndp-stream-v2`): 253 data files in 3 processes and 41 MC files in 6 processes, all 294 files `done` (sidecar entry totals and POT equal to the harvested table), ~20 min wall; harvested to `<data_dir>/products/FHC/1A/` (14 GB per-file archive) and merged: data 8.9689e19 POT_Used, MC 4.0717e20; playlist products `reco_1A_data.npz` 566 MB, `reco_1A_mc.npz` 1.6 GB, `truth_1A_skim.npz` 573 MB, `reco_1A_truthcols_skim.npz` 866 MB. `ndp signal` / `ndp selection` on the products (runs `2026-09-14_{signal,selection}_minerva_me_ccqelike_1mu1p_FHC1A`): fiducial truth signal 223 595; selected 30 049 data vs 35 244.6 scaled MC (data/MC 0.853), MC purity 0.484, efficiency 0.347 (single file: 0.486 / 0.352); playlists 1B–1F submitted next (41 processes) | First full-playlist result of the reproduction; the single-file numbers hold at 41× the statistics, and the data deficit against the unweighted CV MC (0.85) is now measured to ±0.6 % statistical. `ndp signal` on the skims took 15 s and `ndp selection` 37 s | `grid/campaigns/fhc_2026-09/campaign.json`, `/exp/dune/data/users/liangliu/minerva-open-data/products/FHC/1A/`, `runs/2026-09-14_*_FHC1A`

2026-09-14 | grid processing | **Disk request 8 GB for MC processes** (was 4 GB): the three processes of `FHC_mc_1N` were held (`Disk/limit 4334061/4194304`) — 1N files carry 1.65e19 POT each versus 9.9e18 in 1A, so eight of them exceed 4 GB of outputs; data processes stay at 2 GB. Also: harvest gained `--no-archive` (sidecar + reco table + skims only, ~65 MB per MC file; the full truth tables stay on PNFS scratch) after the `/exp/dune/data` user quota (~400 GB) was hit during the 1B–1F harvest — the full-archive policy needs ~150 GB the quota does not have | The held jobs were removed and their files go through `ndp grid resubmit`; the quota incident also truncated `ndp/grid/campaign.py` in the working tree (restored from git) and the local `minerva-open-data` area was deleted by the user (caches rebuilt by streaming, byte-identical) | `ndp/cli.py`, `ndp/grid/campaign.py`
