# Open questions

Decisions the physicist owns and the platform has not settled. Format:
`- [ ] date | scope | question | where it came up`.

## Active

- [ ] 2026-09-04 | minerva channel | **Ratify the beam-frame truth default** (`phase_space.frame: beam`). Evidence in `docs/decisions.md`. Consequence for the exploration repo: runs 2026-06-09 and 2026-06-19 built migration/efficiency with detector-frame truth; re-running their unfolding in the beam frame would test whether the shape χ²/ndf ≈ 3 they report is partly this. | surrogate build, `tests/test_minerva_certification.py`
- [ ] 2026-09-04 | minerva channel | **Which Φ normalises a POT-based MC?** The reference MC normalised with the published ν-e-constrained Φ (6.32e-8) sits +6.0% above the published d²σ (unfolded) and +6.6% above the selected data (folded, data/pred 0.938) — the same −6% pull the exploration repo saw. The generated MC flux (unconstrained gen2thin/g4numiv6) is ~12% above the constrained one per arXiv:2110.13372 §2, and the CV MC carries no flux/tune weights. Decide: published Φ (current), the MC's own generated-flux integral, or apply flux CV weights. | `models/reference_mc.yaml` runs
- [ ] 2026-09-04 | minerva channel | **Feed-in treatment.** Selected signal whose truth is outside the phase space (1.2k of 43k selected MC events) is a POT-scaled additive background in the surrogate. For a model that changes the out-of-phase-space rate this term should scale with the model; a `has_geometry` sample could supply it directly. Acceptable as is? | `ndp/surrogate/binned.py`
- [ ] 2026-09-04 | minerva channel | **n_nucleons**: 3.23e30 (arXiv:2106.16210, adopted by the exploration repo) vs 3.115e30 (arXiv:2110.13372, −2% as-built mass, possibly different fiducial). Same tracker fiducial? | `channels/minerva_me_cc_inclusive_ptpz.yaml`
- [ ] 2026-09-04 | minerva channel | **Target composition for generator runs**: mass fractions from arXiv:2110.13372 §1 (C 88.51 %, H 8.18 %, O 2.5 %, Ti 0.47 %, Cl 0.2 %, Al 0.07 %, Si 0.07 %) are used as GENIE `-t` weights. Confirm this is the intended per-nucleon normalisation target (vs. pure CH). | `ndp/theory/generator.py`
- [ ] 2026-09-04 | surrogate | **Systematic uncertainties** are not in the surrogate (only MC statistics). Flux universes, GENIE knobs, muon-energy scale and MINOS efficiency would enter as alternative response matrices or as covariance on the folded prediction. Which set, and from which weights (the tuple carries `truth_genie_wgt_*` and `mc_wgt_*`)? | `ndp/compare/folded.py`
- [ ] 2026-09-04 | surrogate | **Tails.** The parametric surrogate carries Gaussian cores only; the 1/|q/p| momentum tails are in the binned response but not in the smearing model. Is a flow-type surrogate the next step, or is the binned response sufficient for the channels planned? | `ndp/surrogate/parametric.py`
- [ ] 2026-09-04 | low-recoil channel | `channels/minerva_me_lowrecoil_eavail_q3.yaml` is a draft: exact bin edges from the supplemental Table II, the reco E_avail estimator (exploration repo open question `[~]`), and a manifest kind for the 44-bin release are all needed before it can run. | `channels/`
- [ ] 2026-09-04 | data | The open-data MC generator tag (GENIE 2.12.6 + which tune) is not embedded in the tuple (exploration repo open question). Affects how the `reference_mc` model should be labelled. | `ndp/adapters/minerva_anatuple.py`

- [ ] 2026-09-09 | measurements | **Canonical reco recoil energy.** The example measurements `enu_calorimetric` and `q2_calorimetric` use `MasterAnaDev_recoil_E` (== `_hadron_recoil_CCInc`); the exploration repo's open question `[~]` (three empirical clusters of recoil-E branches, expert confirmation pending) is unresolved. The surrogate is self-consistent for whichever branch is chosen, but a MINERvA-endorsed E_ν/Q² estimator would need that answer (and possibly a calibrated E_ν, e.g. `MasterAnaDev_E`, cached as `reco_anatool_E_nu`). | `measurements/minerva_me_cc_inclusive_ptpz/`
- [ ] 2026-09-09 | measurements | **Edges of the example measurements** (`muon_p_theta`, `enu_calorimetric`, `q2_calorimetric`) are illustrations, `status: example`. Adopt, change or replace them; a changed grid needs `ndp surrogate build --measurement` again. | `measurements/`
- [ ] 2026-09-09 | surrogate | **Per-cell MC statistics on user grids.** A 14×9 muon (p, θ) grid has 114 populated true cells from 43k selected MC events; finer user grids will be MC-limited before they are data-limited at the full playlist. Either cap the grid or move to the event-level (learned) surrogate. | `ndp/surrogate/build.py`
- [ ] 2026-09-14 | 1mu1p channel | **CH fiducial volume and n_nucleons for arXiv:2503.15047.** The paper never states its tracker fiducial (planes/modules, apothem) or a target count (open questions #2, #5 in the extraction). The channel inherits the inclusive channel's vertex box (z 5980–8422 mm, apothem 850 mm) as `status: open`; 5392 of the 21369 cached signal events fall inside it. Decide the fiducial before any absolute comparison. | `channels/minerva_me_ccqelike_1mu1p.yaml`
- [ ] 2026-09-14 | 1mu1p channel | **Which released result is the channel's `published` grid?** 14 one-dimensional CH results exist (δp_T coarse/fine, δp_Tx, δp_Ty, δα_T, φ_T, δp_L, p_n, muon p/p_T/θ, proton p/p_T/θ; edges verified from `anc/tki_release.root`). The placeholder is leading-proton p (5 bins). The TKI observables (δp_T, δα_T, φ_T, δp_L, p_n) are not yet in `ndp.channels.observables`. | `channels/minerva_me_ccqelike_1mu1p.yaml`
- [ ] 2026-09-14 | 1mu1p channel | **Reconstruction-level selection.** The reco cache (v2) carries no proton-candidate columns (`MasterAnaDev_proton_*`, Michel / isolated-cluster counts); the paper's reco proton window (θ_p < 90°, 0.4–1.3 GeV/c) is known only from a commented-out tex line. Which branches define the proton candidate, and are the tuple's Michel and blob counts the paper's? | `ndp/adapters/minerva_anatuple.py`
- [ ] 2026-09-14 | 1mu1p channel | **Events with a second final-state muon** (3 of the 19594 kinematically accepted fiducial events; 4 of 397604 νμ CC in the veto-only parity check) are signal under the primary-lepton definition and background under MAT's `IsQELike` (n_μ == 1). Keep or veto? (1e-4 effect.) | `runs/2026-09-14_signal_minerva_me_ccqelike_1mu1p/summary.json`
- [ ] 2026-09-14 | 1mu1p channel | **Ratify the TKI constants** m_A = 11.174864 GeV, b = 27.13 MeV (m_A' = 10.262429 GeV) in `observable_params.tki` (status default, taken from the LE paper's prescription), and **confirm the δp_Tx sign**: the code uses Lu's (ẑ × p̂_T^μ)·δp_T, which matches the paper's Fig. 1 if the beam points out of the page; the figure's ⊗ marker could also be read as into the page, which would flip δp_Tx (the released grid is symmetric, so only the left–right-asymmetry discussion depends on it). | `channels/minerva_me_ccqelike_1mu1p.yaml`, `ndp/channels/observables.py`
- [ ] 2026-09-14 | grid processing | **Ratify the skim definition**: CC rows inside the enlarged tracker box (z 5880–8522 mm, apothem 900 mm) with derived columns for the 1mu1p channel's frame / proton window / photon threshold. Another channel with a different window or frame needs its own skim (the archives allow re-deriving without re-streaming). | `ndp/adapters/minerva_anatuple.py::DEFAULT_SKIM_BOX`
- [ ] 2026-09-14 | grid processing | **Which playlists enter the analysis** (`data.playlists` in the channel manifest). All 12 ME FHC playlists are processed and merged (campaign `fhc_2026-09`, 1818 data + 489 MC files, 1.0574e21 / 4.9784e21 POT_Used); the committed channel YAML still points at the two single files and the full-FHC runs used a scratch manifest listing 1A–1P. Also open: **storage of the full truth archives** (~150 GB for FHC MC) — they stay on DUNE scratch (`/pnfs/dune/scratch/users/liangliu/ndp-stream/fhc_2026-09/`, subject to eviction) because the `/exp/dune/data` user quota (~400 GB) cannot hold them; the products on disk are the no-archive set (40 GB). | `channels/minerva_me_ccqelike_1mu1p.yaml`, `grid/campaigns/fhc_2026-09/`
- [ ] 2026-09-14 | 1mu1p channel | **Purity gap vs the paper.** With the paper's cut list the MC purity plateaus at 0.49–0.54 for any proton-score threshold (paper: 0.60 at efficiency 0.28; this run 0.49 at 0.35). Remaining background after all cuts: 24% single π±, 12% single π⁰, 6% multi-π, 10% no-pion non-signal. Candidates: the paper's Bragg-peak / end-of-track criterion is stronger than `proton_score1`; its isolated-cluster counter differs from `n_nonvtx_iso_blobs`; its Michel tagger differs from `improved_nmichel`; or the sideband tuning absorbs part of it. Decide which threshold / counters to adopt (`selection.params`, status default). | `runs/2026-09-14_selection_minerva_me_ccqelike_1mu1p_2/summary.json`
- [ ] 2026-09-14 | 1mu1p channel | **Data statistics and MC weights.** The local data file is 2.05e17 POT (64 selected events) against the paper's 1.06e21; the MC is the unweighted CV (no MINERvA-tune / flux / GENIE weights), so the data/MC ratio 0.80 ± 0.10 is a shape check only. Statistics are no longer the limit (full FHC: 329 653 selected data events, data/MC 0.842 ± 0.002 stat); the weight set must be decided before reading normalisations. | `runs/2026-09-14_selection_minerva_me_ccqelike_1mu1p_FHC/`
- [ ] 2026-09-15 | 1mu1p channel | **The factorised efficiency ansatz is not good enough for the transverse-imbalance grids.** Weighting the MC's own truth signal with ε_μ(p_μ, cos θ_μ)·ε_p(p_p, cos θ_p)/⟨ε⟩ reproduces the selected signal to 0.3 % in total and within 2–9 % per bin on the muon and proton kinematics, but by up to 46 % (δp_T), 51 % (δp_Ty), 58 % (p_n) and 46 % (δp_L) per bin: the selection efficiency depends on the muon–proton correlation, which two single-particle maps cannot carry. Use the full binned response (`ndp run ... --measurement all --modes folded`) for those grids, or a 4D map if the user wants a weight-only route. | `runs/2026-09-15_efficiency_minerva_me_ccqelike_1mu1p/ansatz_closure.json`, `report/Efficiency_1mu1p_FHC.md`
- [ ] 2026-09-15 | GiBUU | **Event weights of GiBUU's flux-averaged MC mode are unusable for a QE-dominated selection.** In `nuXsectionMode = 16` the QE cross section is carried by rare very heavy events (55 % of σ_QE above 20× the mean weight; 1 140 QE events have 30 effective ones; a signal sample of 328 events has 21), and the rejection mode (`equalWeights_Mode = 2`) aborts on any weight above its ceiling, which the QE tail exceeds at 500 and 1 500 within a few hundred test nucleons. Options under test: the fixed-energy `integratedSigma` mode on a flux-weighted energy grid, or a very large weighted campaign; the non-QE channels accept 0.6 % at a ceiling of 600 (79 s per 48 000 test nucleons). | `.claude/skills/gibuu/references/running.md` §2.4
- [ ] 2026-09-14 | 1mu1p channel | **Playlist dependence of the MC efficiency and of data/MC.** With the same selection, the MC efficiency falls from 0.347–0.350 (1A–1E) to 0.329–0.333 (1F, 1G), 0.303–0.309 (1L, 1M, 1N) and 0.278–0.279 (1O, 1P) while the purity stays at 0.480–0.486; data/MC rises from 0.83–0.85 (1A–1D) to 0.81–0.83 (1E–1M) and 0.88–0.90 (1N–1P). The truth denominator uses the same fiducial for every playlist, so this is the simulated detector response changing across playlists (dead channels, MINOS matching, or the beam spot) and the data not following it in the same measure; the paper's efficiency 28 % is the average over its playlists. Needs a look at which cut loses the efficiency per playlist before any tune weight is judged. | `runs/2026-09-14_selection_minerva_me_ccqelike_1mu1p_FHC/cutflow.json` (`chunks`)
- [ ] 2026-09-14 | 1mu1p channel | **Containment via `MasterAnaDev_hadron_isExiting[0]`** assumes slot 0 is the proton candidate (93% per the exploration repo's audit); alternatives are the proton end point (`reco_proton_end_z`, x/y) inside the tracker/ECAL or the end-of-track cluster branches (`*_clusters_at_end_proton_prong`, not cached). Keep, replace, or drop (3% effect)? | `channels/minerva_me_ccqelike_1mu1p.yaml`

- [ ] 2026-09-04 | genie | **Spline knots/energy for generated sets.** `make-splines` defaults to 100 log-spaced knots to 100 GeV per nuclide (CVMFS sets use 250 knots to 1 TeV). Enough for the ME flux? The G18_10a_02_11b set is being generated with these defaults. | `scripts/make_splines.sh`

- [ ] 2026-09-04 | adapters | **NuWro cross-section units.** `event.weight` (σ_tot in cm², written on every saved event) is taken as *per nucleon*; the 3 GeV νμ-C12 smoke run gives 2.04e-38 cm², the textbook ~0.7e-38·E_ν per-nucleon value, which supports but does not prove it — confirm against NuWro's `totals.txt` / documentation before absolute comparisons. | `ndp/adapters/nuwro_root.py`
- [ ] 2026-09-04 | adapters | **GiBUU perweight normalisation.** Weights are assumed to sum, per run, to σ_tot in 1e-38 cm²/nucleon (so ÷ n_runs); the adapter records the last row of `neutrino_absorption_cross_section_ALL.dat` next to the file for a cross-check but does not yet enforce agreement. Also: MINERvA-ME flux card for GiBUU (`nuExp`) not yet written. | `ndp/adapters/gibuu_finalevents.py`

- [ ] 2026-09-22 | VBLL surrogate | **Training population of the ported x60-het checkpoint vs the platform's target.** Its training CSV is the Playlist 1D StandardMC selected with a whole-detector, single-hadron-track, truth-meson-poor selection (reco vertex z 4.0–8.6 m, 30 % in the nuclear-target region; 4.5 % true-meson events vs 57 % in the platform-selected events it lacks); only 66,725 of its 253,361 events pass `minerva_ccqelike_1mu1p_v0` and it holds 28 % of the platform-selected rows of 1D. The learned p(reco | truth) therefore mixes passive-target and tracker reconstruction and a purer topology than the platform's 0.49-purity selection. Accept it as the surrogate of record on the strength of the closure steps, or retrain on the platform's own selected signal pairs (plan Step 11)? | `runs/2026-09-22_vbll_surrogate_1mu1p/bench/step2_training_pairs.json`, `bench/step2_selection_scan.json`

## Resolved

- [x] 2026-09-04 | genie | Are GENIE `-t` target-mix weights number or mass fractions? → mass fractions: `GMCJDriver` uses them as density-weighted path lengths and divides by A (`InteractionProbability`), consistent with the H₂O `[0.8888],[0.1111]` convention. Recorded in `docs/decisions.md`.
- [x] 2026-09-04 | flux | Can the MAT `MnvH1D` flux files be read here? → No (no MAT dictionaries; MakeProject needs a compiler the pixi ROOT lacks). The published 2110.13372 Table I is the flux source; integral 0.991 × the published Φ.
